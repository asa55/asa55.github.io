var image = document.getElementById("splash");
new simpleParallax(image, {
                   scale: 0.8,
                   delay: .3,
                   transition: 'cubic-bezier(0,0,0,.1)'
                
                });
name: Continuous Integration

on:
  push:
    branches-ignore:
      - 'main'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:    
    - name: Run npm lint action
      uses: ./.github/actions/lint

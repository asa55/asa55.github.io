name: Continuous Integration

on:
  push:
    branches-ignore:
      - 'main'

jobs:

  lint:
    runs-on: ubuntu-latest
    steps:    
    - uses: actions/checkout@v1
    - name: lint
    - uses: ./.github/actions/lint

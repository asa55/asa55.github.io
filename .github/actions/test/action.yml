name: 'Test'
description: 'Run npm test'
runs:
  using: "composite"
  steps:

    - name: Install dependencies
      shell: pwsh
      working-directory: vite-react-app
      run: npm ci
    - name: Run test
      shell: pwsh
      working-directory: vite-react-app
      run: npm run test

name: 'Build'
description: 'Run npm build'
runs:
  using: "composite"
  steps:
    - name: Install dependencies
      shell: bash
      working-directory: vite-react-app
      run: npm install
    - name: Run build
      shell: bash
      working-directory: vite-react-app
      run: npm run build
